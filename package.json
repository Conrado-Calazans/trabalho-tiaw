{
  "name": "api-livros-render",
  "version": "1.0.0",
  "description": "API de livros com JSON Server para Render",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch src/public/db/db.json --host 0.0.0.0 --port $PORT"
  },
  "author": "Capítulo Aberto",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}