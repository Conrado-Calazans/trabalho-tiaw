{
  "name": "api-livros",
  "version": "1.0.0",
  "description": "API de livros com JSON Server",
  "scripts": {
    "start": "json-server --watch src/public/db/db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
